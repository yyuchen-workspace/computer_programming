#include<stdio.h>
#include<stdbool.h>

int calculate(int max, int number)
{
    int combinations[3] = {};
    int cal = 0;
    for(int i = max, i >  ; i++)
    {
        bool repeat = false;
        temp = 0;
        while(!repeat)
        {
            combination[0] = i;
            for(int j = 1 ; j < number ; j++)
            {

            }
        }

    }
}


int main()
{
    int N, K;

    scanf("%d%d", &N, &K);
    int result = calculate(N, K);
}
