#include<stdio.h>
int main ()
{
    int T, N, M, x, y;
    while(){
        scanf("%d", &T);
        scanf("%d %d", &N, &M);

        while(T != 0) {
            scanf("%d %d", &x, &y );


    }



    }

    return 0;
}
